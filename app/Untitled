


太棒了！既然你已经准备好动手，我们就撇开具体的代码细节，从**上帝视角**把整个项目的**基础设施（硬件与系统）、网络拓扑、以及文件架构**一次性梳理定型。

这就是你的**“数字小提琴实验室（Digital Violin Hub）”**的最终蓝图。你可以按照这个清单，一步步把地基打好。

---

### 第一阶段：硬件与操作系统初始化 (Infrastructure)

这部分的目标是让你的 ThinkPad X1 成为一台安静、稳定、不休眠的服务器。

1.  **系统安装**：
    *   给 X1 装上全新的 **2TB NVMe SSD**。
    *   安装 **Ubuntu Server 24.04 LTS**（如果需要连 Wi-Fi 方便点，装 Desktop 版也可，但日常不需要开显示器）。
2.  **合盖不休眠设置（关键）**：
    *   修改 `/etc/systemd/logind.conf`，将 `HandleLidSwitch=ignore` 启用。
    *   执行 `sudo systemctl restart systemd-logind`。现在你可以把笔记本合上，塞进琴房的角落了。
3.  **大容量硬盘挂载**：
    *   如果是单盘策略，系统和数据都在 2TB 里，直接用即可。
    *   如果是双盘（比如自带 256G 系统盘 + 外加 2TB 数据盘），将 2TB 硬盘永久挂载到 `/data` 目录，并在 `/etc/fstab` 中设置开机自动挂载。
4.  **基础环境准备**：
    *   安装必要的底层工具：`sudo apt update && sudo apt install python3-pip python3-venv ffmpeg -y`（其中 `ffmpeg` 是未来让 AI 读取视频时长、抽帧的底层核心）。

---

### 第二阶段：网络拓扑与外部通信 (Networking)

让所有设备无缝互联，且能安全地对外分享。

1.  **核心内网层 (Tailscale Mesh)**：
    *   在 X1、跑大模型的 PC、iPad、MacBook 上全部登录同一个 Tailscale 账号。
    *   X1 会获得一个固定的虚拟 IP（例如 `100.x.x.10`）。你在任何地方，只要浏览器输入 `http://100.x.x.10:8000` 就能打开你的 Dashboard。
2.  **大模型通信 (AI Link)**：
    *   确保你的 AI 机器（假设 IP 为 `100.x.x.20`）上的 Ollama / vLLM 绑定了 `0.0.0.0`，允许外部网络调用其 API。
    *   在 X1 上测试能否 ping 通，并用 `curl` 测试大模型接口是否畅通。
3.  **公网分享层 (Tailscale Funnel - 可选配置)**：
    *   当你想把某篇技术分享发给老师（老师没有 Tailscale）时，在 X1 上执行 `tailscale funnel 8000`。
    *   这将为你生成一个公共的 HTTPS 域名（如 `https://your-x1.ts.net`），安全暴露你的特定页面。

---

### 第三阶段：文件系统架构 (File System Blueprint)

这是重中之重，是系统的“骨架”。在 `/data`（或你指定的任意 2TB 硬盘目录）下，创建以下极其严格的目录树：

```text
/data/ViolinHub/
│
├── ⚙️ app/                        # 你的 Vibe Coding 代码区
│   ├── main.py                   # FastAPI 核心胶水层
│   ├── venv/                     # Python 虚拟环境 (隔离依赖)
│   ├── db.sqlite3                # 练习打卡与标注的轻量数据库
│   └── static/                   # 前端代码 (index.html, app.js, tailwind)
│
├── 📚 1_Repertoire/               # 静态知识库 (只读，按曲目分文件夹)
│   ├── Brahms_Sym_No4/
│   │   ├── score.pdf             # iPad forScore 直接读取此文件
│   │   └── reference_audio.flac  # 大师版参考音源
│   └── Paganini_Caprice_24/
│
├── 🎬 2_Practice_Logs/            # 动态练习库 (由系统自动管理)
│   ├── 📁 2026-04/               # 按月自动归档
│   │   ├── 20260420_Brahms_110bpm.mp4 
│   │   └── 20260420_Brahms_110bpm.json # 关联的 AI 分析与评价数据
│
├── 📤 3_Upload_Inbox/             # 暂存区 (手机录完随手扔进来)
│   └── (AI 管家脚本每天凌晨扫描这里，低于 30 秒删掉，正常的移入 Log 目录)
│
└── 🗑️ 4_Trash/                    # 回收站 (废弃录像，定时清空)
```

---

### 第四阶段：软件系统架构流 (Architecture Flow)

整个系统只有三个角色，各自职责极其清晰：

1.  **前端展示端 (Client Browser)**
    *   运行在 iPad / Mac 的浏览器中。
    *   只负责两件事：**绘制 UI**（Tailwind）、**发请求**（Fetch API 拿视频流和 AI 回复）。
2.  **核心枢纽端 (ThinkPad X1 - FastAPI)**
    *   **静态服务器**：挂载 `/data/ViolinHub/app/static` 渲染网页。
    *   **流媒体中间件**：非常重要！为了让 4K 视频能丝滑拖拽，FastAPI 需要实现支持 `HTTP 206 Partial Content` (Range 请求) 的视频读取接口。
    *   **AI 代理**：前端发来的对话，FastAPI 接收后，打包发给局域网的 Gemma 机器，再把结果返回前端。
    *   **数据库读写**：写入你的练习时长和打卡记录。
3.  **算力端 (Gemma 4 E4B Machine)**
    *   纯粹的“无情答题机器”，接收 X1 传过来的文本、图片、或提取出的音频，输出 JSON 或 Markdown 格式的分析报告。

---

### 下一步你的执行动作建议（Bootstrap）

现在架构已定，当你准备开始“召唤”代码时，请按照这个顺序进行：

1.  把 X1 硬件和 Ubuntu 装好，Tailscale 连通。
2.  在 X1 上建好上面提到的目录树，随便丢几个测试的 PDF 和 MP4 进去。
3.  打开 Cursor（通过 SSH 连接到 X1），创建一个 Python 虚拟环境。
4.  **第一发 Prompt**：让 AI 帮你写一个支持“断点续传/视频流拖拽读取”的 `main.py` 接口。*(提示：直接告诉 AI “用 FastAPI 写一个支持 Range Header 的视频流接口”，这是看视频丝滑的关键)*。
5.  **第二发 Prompt**：生成前端界面框架。

地基已经打好，剩下的就交给你的创意和大模型的代码生成能力了。开启你的 Vibe Coding 之旅吧！







为了确保你用 Vibe Coding（大模型生成代码）时，能够一次性得到惊艳且完全可用的效果，**绝对不能把所有需求揉成一段话扔给 AI**。

开发这类交互密集的 Dashboard，最稳妥的策略是**“分步生成，搭积木”**。

针对你特别关心的**“老师批注（Teacher Annotation）”**功能，它的核心逻辑是：**“获取视频当前时间戳 (currentTime) -> 暂停 -> 弹框输入评论 -> 保存为 JSON -> 生成带时间戳的跳转按钮”**。

以下我为你设计的 **Dashboard 专属 Prompt 架构**，分为 3 个阶段。你可以直接复制这些 Prompt 到 Cursor 中。

---

### 第一步：搭建基础骨架 (The Skeleton)
*目标：把界面的大分区画出来，确保 Tailwind 样式正确，左侧能读取文件树。*

**直接复制给大模型的 Prompt 1：**
> “我正在使用 FastAPI + 纯 HTML/原生 JS + Tailwind CSS 开发一个小提琴学习 Dashboard。
> 请帮我生成前端的 `index.html` 和 `app.js`。
> 
> **布局要求（暗色模式，专业 UI）：**
> 1. **全屏响应式**：`h-screen w-full flex bg-gray-900 text-white`。
> 2. **左侧边栏 (w-64)**：顶部是 Logo 'Violin Studio'。下方是一个文件树容器 `<div id="file-tree">`。
> 3. **中间主工作区 (flex-1)**：
>    - 垂直分为两块。
>    - 上半部分：占 60% 高度，是一个 `<video id="main-player" controls>` 容器，占据全部空间，黑色背景。
>    - 下半部分：占 40% 高度，是一个 `<iframe id="score-viewer">` 用于显示 PDF 乐谱。
> 4. **右侧边栏 (w-80)**：顶部采用 Tab 切换设计，两个 Tab 为【AI 助教】和【批注/笔记】。
> 
> **JS 逻辑要求：**
> 1. 写一个 `fetchFiles()` 函数，调用后端的 `/api/files` 接口（假设返回格式为 `[{"name": "file.mp4", "path": "/data/..."}]`），并渲染到左侧文件树。
> 2. 点击文件树中的 mp4，改变 `main-player` 的 src。点击 pdf，改变 `score-viewer` 的 src。
> 
> 请给出完整的 HTML 和 JS 代码，无需任何前端构建工具。”

---

### 第二步：注入灵魂 —— 老师/AI 批注系统 (The Annotation System)
*目标：实现你在意的“精准批注”功能。这是交响乐排练复盘的神器。*

等大模型写完第一步且你能正常预览后，**追加这个核心 Prompt 2：**

> “太棒了。现在我们要为右侧的【批注/笔记】Tab 添加‘视频时间戳批注 (Video Annotation)’功能。这是供老师或 AI 留评语用的。
> 
> **请修改 HTML 和 JS，实现以下逻辑：**
> 1. **UI 添加：** 在视频播放器下方（或者右侧批注 Tab 内），加一个【添加批注 (Add Note)】按钮。
> 2. **交互逻辑：**
>    - 点击【添加批注】时，视频自动暂停 (`video.pause()`)。
>    - 获取视频当前的时间戳，精确到小数点后 1 位（例如 `01:15.4`）。
>    - 弹出一个极简的输入框让老师输入文字（例如：“这里的换把有杂音”）。
> 3. **数据展示：** 
>    - 输入完成后，将这个批注渲染在右侧的【批注列表】中。
>    - UI 样式必须清晰：左边是一个带有下划线的蓝色时间戳链接（如 `[01:15.4]`），右边是评语内容。
> 4. **核心联动：**
>    - 当用户点击批注列表中的 `[01:15.4]` 时，主播放器的 `currentTime` 立刻跳转到该时间并开始播放 (`video.play()`)。
> 5. **数据存储：** 写一个占位的 `saveAnnotation()` 函数，将这条包含 `{video_id, timestamp, text, author}` 的对象通过 POST 请求发给后端的 `/api/annotations` 接口。
> 
> 请重点确保时间戳格式化函数和 currentTime 跳转逻辑的精准性。”

---

### 第三步：AI 对话与极客工具集成 (AI & Tools)
*目标：把 Gemma 接入进来，并增加你的实用工具。*

**追加 Prompt 3：**

> “现在完善右侧的【AI 助教】Tab，并添加实用工具。
> 
> **功能 1：AI 聊天框**
> 1. 在 AI 助教 Tab 下画一个类似 ChatGPT 的对话界面。
> 2. 有一个输入框和发送按钮。发送文字时，调用后端的 `/api/chat`。
> 3. **关键上下文：** 发送请求时，自动携带当前正在播放的视频文件名，拼接成格式发给后端，例如 `[当前视频: Brahms.mp4] 我的问题是...`。
> 
> **功能 2：顶部快捷工具栏**
> 1. 在中间工作区的最上方，加上极简的 Toolbar。
> 2. 加入一个【A-B 循环】功能按钮组：
>    - 按钮【设为 A 点】：点击记录当前视频时间为 A。
>    - 按钮【设为 B 点】：点击记录当前时间为 B。
>    - 开启循环后，JS 监听视频的 `timeupdate` 事件，如果 `currentTime >= B`，自动跳转回 A 点播放。这对于死磕管弦乐困难片段非常重要。
> 3. 加入一个【镜像翻转】按钮：点击后，利用 CSS 的 `transform: scaleX(-1)` 将视频画面左右翻转（用于对比老师和自己的持琴动作）。
> 
> 请给出更新后的 JS 代码和必要的 HTML 结构。”

---

### 💡 给你的进阶“防翻车”建议（Vibe 技巧）

1. **先做假数据（Mock Data）**：
   在开始写 Python 后端之前，你可以让前端跑通。只需要在 Prompt 里告诉 AI：“*目前后端还没写，请在 JS 里写一些 Mock 的文件树 JSON 和批注 JSON 来测试渲染效果。*” 这样你能直观看到老师的批注长什么样。
2. **处理跨域（CORS）与视频拖拽卡顿（206 Partial Content）**：
   当大模型帮你写 Python `main.py` 的时候，**一定要加这句话**：
   > “请使用 fastapi，提供一个 `/video/{filepath}` 接口。请注意，这个接口**必须支持 HTTP Range Requests (206 Partial Content)**，否则前端的 HTML5 播放器无法随意拖拽进度条和进行时间戳跳转。”
   （*注：这是很多人自己写后端时踩的大坑，告诉大模型这个关键词，它秒懂。*）
3. **老师如何批注？**
   在实际使用时，老师只需用浏览器打开你分享的链接（比如通过 Tailscale Funnel），点击你上传的视频。看到问题，直接按个快捷键（你可以让大模型加个监听，比如按 `N` 键新建批注），视频暂停，输入“音准偏低”，回车。这批数据就永久存进了你 X1 的数据库，并绑定了这个视频的具体秒数。

按照这 3 个 Prompt 去向 Cursor 或 Claude 下达指令，你会发现写出一个专业级的多媒体排练 Dashboard，其实只需要几分钟的对话。如果你在生成“A-B循环”或者“视频流”时遇到报错，随时把报错贴给我，我帮你微调 Prompt！